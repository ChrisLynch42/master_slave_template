<table>
  <tr>
    <th>Title</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @races.each do |race| %>
  <tr>
    <td><%= race.title %></td>
    <td><%= race.description %></td>
    <td><%= link_to 'Show', race, :class => 'ajaxLink'  %></td>
    <td><%= link_to 'Edit', edit_race_path(race), :class => 'ajaxLink' %></td>
    <td><%= link_to 'Destroy', race_path(race), :class => 'ajaxDeleteLink' %></td>
  </tr>
<% end %>
</table>
