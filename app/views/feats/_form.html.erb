<%= form_for(@feat) do |f| %>
  <% if @feat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@feat.errors.count, "error") %> prohibited this feat from being saved:</h2>

      <ul>
      <% @feat.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field :character_id %>
  </div>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description, {:rows=>0, :cols=>0, :class => 'smallDescriptionArea' } %>
  </div>
  <div class="field">
    <%= f.label :prerequisite %><br />
    <%= f.text_area :prerequisite , {:rows=>0, :cols=>0, :class => 'smallDescriptionArea' } %>
  </div>
  <div class="field">
    <%= f.label :benefit %><br />
    <%= f.text_area :benefit , {:rows=>0, :cols=>0, :class => 'smallDescriptionArea' } %>
  </div>
  <div class="actions">
    <%= f.submit nil, :class => 'ajaxFeatSubmit' %>
  </div>
<% end %>
