<%= form_for(@weapon) do |f| %>
  <% if @weapon.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@weapon.errors.count, "error") %> prohibited this weapon from being saved:</h2>

      <ul>
      <% @weapon.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :character_id %>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description, {:rows => 0, :cols => 0, :class => 'smallDescriptionArea'} %>
  </div>
  <div class="field">
    <%= f.label :damage %><br />
    <%= f.text_field :damage %>
  </div>
  <div class="field">
    <%= f.label :attack_bonus %><br />
    <%= f.number_field :attack_bonus %>
  </div>
  <div class="field">
    <%= f.label :damage_bonus %><br />
    <%= f.number_field :damage_bonus %>
  </div>
  <div class="actions">
    <%= f.submit nil, :class => 'ajaxSubmit' %>
  </div>
<% end %>
