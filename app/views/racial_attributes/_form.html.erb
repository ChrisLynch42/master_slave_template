<%= form_for(@racial_attribute) do |f| %>
  <% if @racial_attribute.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@racial_attribute.errors.count, "error") %> prohibited this racial_attribute from being saved:</h2>

      <ul>
      <% @racial_attribute.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field :character_id %>
  </div>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description, {:rows=>0, :cols=>0, :class => 'smallDescriptionArea' }%>
  </div>
  <div class="actions">
    <%= f.submit nil, :class => 'ajaxRacialAttributeSubmit' %>
  </div>
<% end %>
